Todo's for RELEASE

Wordsmith Text
Correct URLS
Make use of xincludes whenever possible
Get 2.6.15 kernel in
Fix udev when 2.6.15 goes in
Create new bootscripts for udev
package changes to udev in bootscripts
Get people to test builds
Try to fix, or else document, failures in binutils testsuites (e.g x86_64-64)
Try to fix, or else document, failures in glibc testsuites (e.g. 32-bit on
 multilib)
Documents any other testsuite failures. 

----
Need What they are
